name: OpenCode
run-name: "OpenCode #${{ github.event.issue.number || inputs.issue_number }}${{ github.event.issue.title && format(' — {0}', github.event.issue.title) || '' }}"

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: Existing issue to run
        required: true
        type: string

permissions: {}

concurrency:
  group: opencode-issue-${{ github.repository }}-${{ github.event.issue.number || inputs.issue_number }}
  cancel-in-progress: false

jobs:
  prepare:
    if: github.event_name == 'workflow_dispatch' || contains(github.event.issue.labels.*.name, 'OpenCode') || contains(github.event.issue.title, '/OpenCode')
    permissions:
      contents: read
      issues: write
      actions: write
    uses: AgentsLoop/OhMyGithub/.github/workflows/opencode-prepare.yml@7fbc0c2de845c1da8cf5edd2185b117d3ad2963b
    with:
      runtime_ref: 7fbc0c2de845c1da8cf5edd2185b117d3ad2963b

  opencode:
    needs: prepare
    if: needs.prepare.outputs.approved == 'true'
    permissions:
      contents: write
      issues: write
      actions: read
    uses: AgentsLoop/OhMyGithub/.github/workflows/opencode-reusable.yml@7fbc0c2de845c1da8cf5edd2185b117d3ad2963b
    with:
      runtime_ref: 7fbc0c2de845c1da8cf5edd2185b117d3ad2963b
      issue_number: ${{ needs.prepare.outputs.issue_number }}
      request: ${{ needs.prepare.outputs.request }}
      issue_title: ${{ needs.prepare.outputs.issue_title }}
      labels_json: ${{ needs.prepare.outputs.labels_json }}
      sender: ${{ needs.prepare.outputs.sender }}
      target_ref: ${{ needs.prepare.outputs.target_ref }}
      target_sha: ${{ needs.prepare.outputs.target_sha }}
    secrets:
      OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
      OPENCODE_AUTH_JSON: ${{ secrets.OPENCODE_AUTH_JSON }}
      AGENTSWEB_SSH_PUBLIC_KEY: ${{ secrets.AGENTSWEB_SSH_PUBLIC_KEY }}
